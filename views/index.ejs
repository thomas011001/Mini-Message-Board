<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/output.css" />
    <title>Message Board!</title>
  </head>
  <body>
    <div class="max-w-[800px] mx-auto">
      <%- include('nav', {link: {text: "Add new message", path: "/new"}}) %>
      <ul class="flex flex-col gap-5">
        <% messages.map((message) => { %>
        <li class="border-[1px] p-3 border-black">
          <div>
            <span class="font-bold"><%= message.user %>:</span>
            <%= message.text %>
          </div>
          <div class="font-light text-sm"><%= message.added %></div>
          <a
            href="/message/<%= message.id %>"
            class="text-blue-600 hover:underline decoration-blue-600"
            >Open</a
          >
        </li>
        <% }) %>
      </ul>
    </div>
  </body>
</html>
